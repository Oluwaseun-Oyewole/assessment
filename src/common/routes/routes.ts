export const enum Routes {
  base = "/",

  // products
  dashboard = "/app",
  home = "/app/home",
  reports = "/app/reports",
  chat = "/app/chats",
  budget = "/app/budget",
  profile = "/app/profile",
}
